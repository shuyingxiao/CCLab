window.addEventListener('scroll', function () {
  // 获取滚动距离
  const scrollTop = window.scrollY;
  // 将盒子的偏移量设置为滚动距离的一半
  const box = document.querySelector('.one');
  //secondBox.style.transform = `translateY(${scrollTop * (1)}px)`
  one.style.transform = `translateX(${scrollTop * (-4)}px)`


});

window.addEventListener('scroll', function () {
  // 获取滚动距离
  const scrollTop = window.scrollY;
  // 将盒子的偏移量设置为滚动距离的一半
  const box = document.querySelector('.two');
  //secondBox.style.transform = `translateY(${scrollTop * (1)}px)`
  two.style.transform = `translateX(${scrollTop * (-3.5)}px)`


});
window.addEventListener('scroll', function () {
  // 获取滚动距离
  const scrollTop = window.scrollY;
  // 将盒子的偏移量设置为滚动距离的一半
  const box = document.querySelector('.three');
  //secondBox.style.transform = `translateY(${scrollTop * (1)}px)`
  three.style.transform = `translateX(${scrollTop * (-3)}px)`


});

window.addEventListener('scroll', function () {
  // 获取滚动距离
  const scrollTop = window.scrollY;
  // 将盒子的偏移量设置为滚动距离的一半
  const box = document.querySelector('.four');
  //secondBox.style.transform = `translateY(${scrollTop * (1)}px)`
  four.style.transform = `translateX(${scrollTop * (-2.5)}px)`


});

window.addEventListener('scroll', function () {
  // 获取滚动距离
  const scrollTop = window.scrollY;
  // 将盒子的偏移量设置为滚动距离的一半
  const box = document.querySelector('.five');
  //secondBox.style.transform = `translateY(${scrollTop * (1)}px)`
  five.style.transform = `translateX(${scrollTop * (-2)}px)`


});
window.addEventListener('scroll', function () {
  // 获取滚动距离
  const scrollTop = window.scrollY;
  // 将盒子的偏移量设置为滚动距离的一半
  const box = document.querySelector('.six');
  //secondBox.style.transform = `translateY(${scrollTop * (1)}px)`
  six.style.transform = `translateX(${scrollTop * (-1.5)}px)`


});


window.addEventListener('scroll', function () {
  // 获取滚动距离
  const scrollTop = window.scrollY;
  // 将盒子的偏移量设置为滚动距离的一半
  const box = document.querySelector('.seven');
  //secondBox.style.transform = `translateY(${scrollTop * (1)}px)`
  seven.style.transform = `translateX(${scrollTop * (-1)}px)`


});